// Disabled legacy static producer analytics widget. The dashboard route now
// opens API-backed product pages, so this component intentionally renders
// nothing until a documented analytics API exists.
const ProducerDashboardWidget = () => null;

export default ProducerDashboardWidget;
